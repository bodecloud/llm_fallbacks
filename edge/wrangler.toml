name = "llm-fallbacks-proxy"
main = "src/index.ts"
compatibility_date = "2025-01-01"

[vars]
ALLOWED_ORIGINS = "https://bolabaden.github.io,http://127.0.0.1:5500,http://localhost:5500"
MAX_TOKENS_CAP = "1024"
MODEL_CHAIN = "openrouter/free"

# Secrets (set via `wrangler secret put` or CI):
# PROXY_GUEST_TOKEN, OPENROUTER_API_KEY, GROQ_API_KEY
