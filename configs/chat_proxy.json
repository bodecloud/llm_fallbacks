{
  "endpoints": [],
  "guestToken": "llm-fallbacks-public",
  "description": "Zero-config demo proxy for GitHub Pages chat. Endpoints are filled by deploy-proxies when the Cloudflare Worker is deployed."
}
