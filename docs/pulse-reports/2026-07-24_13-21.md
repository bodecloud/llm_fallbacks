# llm-fallbacks Pulse — 24h — 2026-07-24 13:21 CDT

## Headlines

- Gateway is live: Worker health OK, stream fallback returns SSE chunks, Pages UI serves dark-theme build (HTTP 200 in ~139ms).
- Heavy shipping day: 14 Python Package runs (13 green), Deploy Pages turned green after webui/proxy fixes (5/8 success in window).
- OpenRouter on the demo key is intermittent — stream path falls back to Workers AI; non-stream sometimes hits OpenRouter directly.

## Usage

- **Primary engagement (homepage sessions):** no data (instrumentation pending)
- **Value realization (first successful chat completion):** no data (instrumentation pending)
- **Strategy metrics:**
  - Gateway success rate: proxy smoke + live Playwright 4/4 pass on latest Pages deploy; prior window had 502 Workers AI failures (now resolved)
  - Config freshness lag: ~16h since daily `free_models.json` commit (2026-07-24 02:35 UTC)
  - Homepage engagement: no data (instrumentation pending)
  - Free-tier cost: $0 recurring (Workers AI + OpenRouter free tier; OpenRouter 429 observed earlier today)

## System performance

- **Latency:** Pages TTFB ~139ms (single sample); Worker stream first chunk <1s (smoke test)
- **Top errors** (CI + proxy, last 24h):
  1. **Deploy Proxies — Cloudflare auth 10000** — 4 failed runs — stale/wrong API token before cfat secret refresh
  2. **Deploy Proxies — wrangler deploy exit 1** — 3 failed runs — same auth window
  3. **Deploy Pages — live e2e Workers AI 502** — 3 failed runs — pre-fix fallback chain / response parsing
  4. **OpenRouter 429 free-models-per-day** — observed on proxy during incident — triggers Workers AI fallback on stream
  5. **Dependabot — 8 repo vulnerabilities** — 2 high, 4 moderate — unrelated to gateway runtime

## Followups

- Add privacy-preserving client event for "first successful completion" to measure STRATEGY homepage engagement metric.
- Wire `pulse_analytics_source` when PostHog or similar is chosen; until then, pulse stays CI + smoke-test based.
- Schedule weekly pulse via GitHub Actions `workflow_dispatch` or cron after analytics lands.
- Monitor OpenRouter daily quota on the shared demo key; document reset time in `docs/solutions/`.
- Clear Dependabot high-severity items when convenient (not blocking gateway).

---
_Source windows: CI/GitHub Actions [2026-07-23 18:05 UTC → 2026-07-24 18:05 UTC], live smoke tests [2026-07-24 18:20 UTC]. Trailing buffer: 15m. No PostHog/Sentry configured. Saved to `docs/pulse-reports/2026-07-24_13-21.md`._
